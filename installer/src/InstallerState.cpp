// InstallerState.cpp
// InstallerState is currently header-only (see InstallerState.h); this file
// exists so the class has a translation unit to live in once it grows
// methods that shouldn't be inlined (e.g. validation logic).

#include "InstallerState.h"
